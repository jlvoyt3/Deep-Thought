//SE 300 Code Repository

//MainMenu contains the bulk of the code, it is responsible for creating the GUI and running most of the pong game
//InstructionsOptions creates a method for the HelpPage
//HelpPage includes the instructions for pong and utiliezes the InstrucitonsOptions method
//IterationTracker  tracks each iteration of the AI's learning algorithim
//AlertBox creates a method for mainMenu so that users are prompted when trying to start the game without picking their options
//ScorePage creates the stage and scene for checking the top scores, it also tracks and determines a user's score
//Iterations of the AI will be tracked using the Iteration Tracker
